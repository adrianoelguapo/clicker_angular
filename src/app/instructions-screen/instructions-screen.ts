import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

/* --- Definir el componente de la vista de cómo jugar --- */
@Component({

  selector: 'app-instructions-screen',
  imports: [RouterModule],
  templateUrl: './instructions-screen.html',
  
})

/* --- Clase del componente de la vista de cómo jugar --- */
export class InstructionsScreen {

  /* --- Como no tiene lógica, no necesita ni métodos ni atributos --- */

}